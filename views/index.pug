extends layout

block content
  //- table
  //-   -for(var i = 0; i < data.length; i++)
  //-     tr.container(onClick=`location.href="${data[i].url}"`)
  //-       td.left-td(width="8%")
  //-         -var siteColor = (data[i].site === '오유' ? 'ou' : null)
  //-         .left-wrap
  //-           .heart ♥
  //-           .like #{data[i].like}
  //-         .left-wrap(class=siteColor)
  //-           div #{data[i].site}
  //-       td(width="*")
  //-         .center-wrap
  //-           .title #{data[i].title}
  .box-container(style="background-color: black; color: white;")
    .left-container 
      .like-wrap
        .like Like
        .view View
      .site site
    .right-container
      .title-wrap 
        text.title(style="color: white;") Title
        span.comment
      .author atuhor / date
  -for(var i = 0; i < data.length; i++)
    -var siteColor = (data[i].site === '오유' ? 'ou' : null)
    a.box-container(href=`${data[i].url}`)
      .left-container
        .like-wrap
          .heart 
          .like ♥#{data[i].like}
          .view #{data[i].view}
        .site(class=siteColor) #{data[i].site}
      .right-container
        .title-wrap
          text.title #{data[i].title} 
          span.comment #{data[i].comment}
        .author #{data[i].author} / #{data[i].date}
  -for(var i = 0; i < data2.length; i++)
    -var siteColor = (data2[i].site === '갣립' ? 'dog' : null)
    a.box-container(href=`${data2[i].url}`)
      .left-container
        .like-wrap
          .heart 
          .like ♥#{data2[i].like}
          .view #{data2[i].view}
        .site(class=siteColor) #{data2[i].site}
      .right-container
        .title-wrap
          text.title #{data2[i].title} 
          span.comment #{data2[i].comment}
        .author #{data2[i].author} / #{data2[i].date}
        